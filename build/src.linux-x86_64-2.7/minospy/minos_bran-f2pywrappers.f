C     -*- fortran -*-
C     This file is autogenerated with f2py (version:2)
C     It contains Fortran 77 wrappers to fortran functions.

      subroutine f2pyinitshanks(setupfunc)
      external setupfunc
      real*8 b(46)
      real*8 c(10)
      real*8 dx
      real*8 step(8)
      real*8 stepf
      integer maxo
      integer in
      common /shanks/ b,c,dx,step,stepf,maxo,in
      call setupfunc(b,c,dx,step,stepf,maxo,in)
      end

      subroutine f2pyinitbits(setupfunc)
      external setupfunc
      real*8 pi
      real*8 rn
      real*8 vn
      real*8 wn
      real*8 w
      real*8 wsq
      real*8 wray
      real*8 qinv
      real*8 cg
      real*8 wgrav
      real*8 tref
      real*8 fct
      real*8 eps
      real*8 fl
      real*8 fl1
      real*8 fl2
      real*8 fl3
      real*8 sfl3
      integer jcom
      integer nord
      integer l
      integer kg
      integer kount
      integer knsw
      integer ifanis
      integer iback
      common /bits/ pi,rn,vn,wn,w,wsq,wray,qinv,cg,wgrav,tref,fct,
     &eps,fl,fl1,fl2,fl3,sfl3,jcom,nord,l,kg,kount,knsw,ifanis,iback
      call setupfunc(pi,rn,vn,wn,w,wsq,wray,qinv,cg,wgrav,tref,fct
     &,eps,fl,fl1,fl2,fl3,sfl3,jcom,nord,l,kg,kount,knsw,ifanis,iback)
      end

      subroutine f2pyinitmtab(setupfunc)
      external setupfunc
      real*8 we(2)
      real*8 de(2)
      integer ke(2)
      real*8 wtry
      real*8 bm
      common /mtab/ we,de,ke,wtry,bm
      call setupfunc(we,de,ke,wtry,bm)
      end

      subroutine f2pyiniteifx(setupfunc)
      external setupfunc
      real*8 a(14,1000)
      real*8 dum(1000)
      common /eifx/ a,dum
      call setupfunc(a,dum)
      end

      subroutine f2pyinitc_buf(setupfunc)
      external setupfunc
      integer nn
      integer ll
      real*4 ww
      real*4 qq
      real*4 gc
      real*4 buf(6000)
      common /c_buf/ nn,ll,ww,qq,gc,buf
      call setupfunc(nn,ll,ww,qq,gc,buf)
      end

      subroutine f2pyinitrindx(setupfunc)
      external setupfunc
      integer nic
      integer noc
      integer nsl
      integer nicp1
      integer nocp1
      integer nslp1
      integer n
      common /rindx/ nic,noc,nsl,nicp1,nocp1,nslp1,n
      call setupfunc(nic,noc,nsl,nicp1,nocp1,nslp1,n)
      end

      subroutine f2pyinit_BLNK_(setupfunc)
      external setupfunc
      real*8 r(1000)
      real*8 fmu(1000)
      real*8 flam(1000)
      real*8 qshear(1000)
      real*8 qkappa(1000)
      real*8 xa2(1000)
      real*8 xlam(1000)
      real*8 rho(1000)
      real*8 qro(3,1000)
      real*8 g(1000)
      real*8 qg(3,1000)
      real*8 fcon(1000)
      real*8 fspl(3,1000)
      real*8 lcon(1000)
      real*8 lspl(3,1000)
      real*8 ncon(1000)
      real*8 nspl(3,1000)
      real*8 ccon(1000)
      real*8 cspl(3,1000)
      real*8 acon(1000)
      real*8 aspl(3,1000)
      common r,fmu,flam,qshear,qkappa,xa2,xlam,rho,qro,g,qg,fcon,f
     &spl,lcon,lspl,ncon,nspl,ccon,cspl,acon,aspl
      call setupfunc(r,fmu,flam,qshear,qkappa,xa2,xlam,rho,qro,g,q
     &g,fcon,fspl,lcon,lspl,ncon,nspl,ccon,cspl,acon,aspl)
      end

      subroutine f2pyinitarem(setupfunc)
      external setupfunc
      real*8 a(6,3,1000)
      common /arem/ a
      call setupfunc(a)
      end


